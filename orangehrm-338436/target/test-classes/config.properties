# OrangeHRM Cucumber Test Configuration
# These values can be overridden by environment variables or system properties.

# Base URL of the OrangeHRM application under test
# Override with: -DORANGEHRM_BASE_URL=http://your-server:port
# Or environment variable: ORANGEHRM_BASE_URL
ORANGEHRM_BASE_URL=http://localhost:8080

# Default credentials for login tests
# Override with environment variables: ORANGEHRM_USERNAME, ORANGEHRM_PASSWORD
ORANGEHRM_USERNAME=Admin
ORANGEHRM_PASSWORD=admin123

# Browser configuration (chrome or firefox)
browser=chrome

# Headless mode (true/false) - set to true for CI environments
headless=false
