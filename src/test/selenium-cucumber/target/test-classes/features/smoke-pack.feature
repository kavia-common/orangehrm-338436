@smoke @critical
Feature: OrangeHRM Smoke Pack (Selenium-Java mirror)

  # Scenarios are implemented in step definitions:
  # Copy Gherkin scenarios here if running tests via this module.
  # For now, the step definitions correspond to those in the Cypress suite.

  # (You can copy from cypress/e2e/integration/feature/smoke/smoke-pack.feature as needed)
